import { WorkoutStateColorPipe } from './workout-state-color.pipe';

describe('WorkoutStateColorPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkoutStateColorPipe();
    expect(pipe).toBeTruthy();
  });
});
