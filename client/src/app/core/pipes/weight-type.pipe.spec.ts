import { WeightTypePipe } from './weight-type.pipe';

describe('WeightTypePipe', () => {
  it('create an instance', () => {
    const pipe = new WeightTypePipe();
    expect(pipe).toBeTruthy();
  });
});
